<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auction</title>
    <style>
        #contain{
            text-align: center;
        }
        input{
            border-color: #ee4c7c;
            width: 200px;
            padding: 10px;
            border-radius: 5px;
        }
        input:hover{
            border-color: #f31658;
            width: 250px;
        }
        input:active{
            border-color: #f31658;
            width: 250px;
        }
        button{
            padding: 7px;
            border-color: #df019c;
            color: #f31658;
            border-radius: 5px;
            font-size: large;
        }
        button:hover{
            padding: 12px;
            background-color: black;
            border-radius: 5px;

        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <% let i,j %>
    <% i = products.price.length -1  %>  
    <% j = products.price[i] %> 
    <div id = "contain">
        <h1 style="color: #9a1750;">Last Bid : <%= products.price[i] %> </h1>
        
        <form action="/bid/<%= products._id %> " method="POST"  >
            <div >
                <label for="price"><h1 style="color: #da099b;"> Your Bid : </h1></label>
                <% if(products.price.length == 0) {%> 
                <input  type="number" name="price"  id="price" placeholder = "<%= products.baseprice %>"  min = "<%= products.baseprice %>" required />
                <% } else { %>    
                    <input  type="number" name="price"  id="price" placeholder = "<%= products.price[i] %>"  min = "<%= j %>" required />  
                <% } %> 
            </div>
            <br>
            <br>
            <button>Bid Here</button>
        </form>
    </div>
</body>
</html>